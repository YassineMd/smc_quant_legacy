# FORWARD LEDGER — frozen configurations under test on forward tape

Freeze discipline: entries here are NEVER re-tuned. Each new snapshot re-runs them unchanged and
appends results; a config graduates or dies on accumulated forward sample only (n >= 20 per side).

| id | registered | fires (frozen) | entry (frozen) | exits (frozen) | fee basis | status |
|---|---|---|---|---|---|---|
| S5E-SIGDEATH | 2026-07-03 (2ec478a) | S5b locked+unlocked legs 1'-4 + EXISTS-form range context (S5d) | market at fire close | signal-death (newest marker against + own-side share < 50, AND) with hard SL -0.3, 6h cap | taker/taker 0.10 | 3/4 cells net-positive on spent tape; awaiting forward n |
| S5H-CONDROUTER | 2026-07-04 (this commit) | locked legs 1'-4 + ALL-form pullback leg 5 + cluster rule (anchor 21340 must stay rejected) | ROUTER: close beyond baseline -> MARKET at close; else WAIT 30 min for moving-baseline touch (taker) | arm A fixed TP+0.5/SL-0.3; arm B signal-death locked | taker/taker 0.10 | registered; ~1 setup/day; underpowered until forward n >= 20 |
| PIVOT-P2HELD | 2026-07-06 (this commit) | S5j-r5 confluence via app/pivot_detect (the SHIPPED indicator), INDEPENDENT per-side sequential walk | WAIT-baseline-touch, taken ONLY IF the ALIGNED live eff-agg (panel-2) spread @E > 0 AND its min over [D,E] > -50 (`p2_live_at_e`>0 & `p2_live_min_de`>-50 in the CSV) | fixed TP+0.5/SL-0.3, 6h cap | taker/taker 0.10 | in-sample (Jul2-5, n=116) KEEP 47 TP 63.8% net +0.117% vs 44.0% base, Fisher p=0.0003; the STRONG candidate; awaiting forward n>=20/side |
| PIVOT-ABSORB-E | 2026-07-06 (this commit) | S5j-r5 confluence via app/pivot_detect, INDEPENDENT per-side sequential walk | WAIT-baseline-touch, taken ONLY IF the entry bar is an ALIGNED absorption candle (long: sell-led `sv>bv` & closed up; short: buy-led & closed down; `entry_absorption`==1 in the CSV) | fixed TP+0.5/SL-0.3, 6h cap | taker/taker 0.10 | in-sample KEEP 41 TP 48.8% vs 44.0% base, Fisher p=0.56 (DIRECTIONAL only, underpowered); awaiting forward n |
| PIVOT-4HZONE | 2026-07-06 (this commit) | S5j-r5 via app/pivot_detect, independent per-side walk | WAIT-baseline-touch, taken ONLY IF the pivot's DETECTION price sits in the last-COMPLETED 240x/4h buy/sell zone: buy -> lower/buyer wick (price <= vq_lo), sell -> upper/seller wick (price >= vq_hi); zones = `bar_quantiles.vq()` of the tf='4h' stored buckets (== the live SSH 240x stream) | fixed TP+0.5/SL-0.3, 6h cap | taker/taker 0.10 | in-sample (Jun28-Jul5, n=116) IN-ZONE 40 TP 62.5% vs OUT 34.2%, **Fisher p=0.006, both-sided (L 60/34, S 65/34)**; INDEPENDENT source (4h vol profile, not 1m eff-agg) -> the strongest candidate besides P2-held; awaiting forward n>=20/side |
| PIVOT-E2-TIER | 2026-07-07 (this commit) | S5j-r5 confluence via app/pivot_detect, INDEPENDENT per-side walk | TIERED by D fill = aligned LIVE panel-2 spread at D (p2d): **cyan/orange** (p2d>80) -> **E2**; **green/red** (63<p2d<=80) -> **E2**; **hollow** (p2d<=63) -> **E if panel-2 HELD** (aligned live spread @E>0 AND min over [D,E]>-50) **ELSE E2**. E2 = flip-rescue: E greyed (@E<=0 or min[D,E]<=-50) then first bar within 1h of E whose aligned live spread re-confirms >= 30 | fixed TP+0.5/SL-0.3, 6h cap | taker/taker 0.10 | in-sample (Jul2-6, 1m, n=106) TP 66.0% net **+0.128%** vs raw -0.052%; **POST-HOC** (63/80 tiers + E2thr=30 + per-tier entry picks ALL fit on this tape) -> heavily optimistic; forward tape is the ONLY test; awaiting forward n>=20/side |
| PIVOT-ZZTRAIL | 2026-07-07 (this commit) — **CURRENT WORKING STRATEGY** | S5j-r5 confluence via app/pivot_detect, INDEPENDENT per-side walk (same fires as PIVOT-E2-TIER) | SAME as PIVOT-E2-TIER (cyan/orange & green/red -> E2; hollow -> E-held-else-E2); take **ALL** E2-tier trades, no entry/risk filter | **ZigZag(0.20%) structural trailing, NO fixed TP**: initial SL 0.1% below last LL / above last HH; trail to 0.05% below each newly-confirmed HL / above each LH; ride to stop. PLUS **breakeven lock**: once +0.40% MFE touched, arm stop at +0.10% (effective stop = max of struct/trail/lock) | taker/taker 0.10 | in-sample (1m, n=112, ~9d) **+20.84%** cum, **+0.186%/trade, 54.5% net win, PF 3.14, t=3.90**; lock ROBUST across a 0.35-0.40 x 0.05-0.10 plateau; but E2-tier entries + exit ALL post-hoc on this tape -> **forward is the only test**. FROZEN 2026-07-07 10:38 UTC (freeze_ts 1783420714, idx 31636). Pre-declared: PASS n>=40 & net>0 & t>=1.5; FAIL n>=40 & net<=0; degrade-warn n>=30 & net<+0.093. Entry-only linchpin (raw fires, fixed exit) net-win baseline = 42.9%. |
| PIVOT-DTIER-ZONE (SIZE overlay) | 2026-07-08 (uncommitted) | SAME S5j-r5 fires as PIVOT-ZZTRAIL | SAME E2-tier entries + ZZTRAIL exit (POSITION-SIZE overlay, not a new fire/entry/exit rule) | overlay keyed on D-badge fill x zone-STATE of the detection price vs the last-completed 4h bucket's two wick bands (GREEN buy [low,vq_lo] / RED sell [vq_hi,high]). FILLED D = cyan/orange (p2d>80) OR red/green (63<p2d<=80). State: WITH = own zone (long in green / short in red); OPPOSITE = other zone (long in red / short in green); NEITHER = body. **SIZE UP** filled-D + WITH; **SKIP** filled-D + OPPOSITE; normal filled-D + NEITHER; **hollow = zone-agnostic, normal size** | taker/taker 0.10 | in-sample (n=112, three-outcome NET): filled-D WITH = **8/8 winners** (net +0.337%); filled-D OPPOSITE = **trap 4/7 losers 57%, net −0.102% (−$7)**; filled-D NEITHER = coin flip (5W/5L, +$9). Hollow zone-agnostic (WITH 50%/OPP 40.7%/NEITHER 47%W, all ~base). Cyan/orange alone = WEAKEST tier by total ($11) — the zone direction does all the work. **Tiny n (7-8 filled cells)** -> forward tape is the only test; graduate at n>=20/side. Study: study/pivot_dtier_zone.py, pivot_dtier_zone3.py |
| PIVOT-COMPOSITE-ZONE (TAKE/SIZE overlay) | 2026-07-08 (uncommitted) | SAME S5j-r5 fires as PIVOT-ZZTRAIL | SAME E2-tier entries + ZZTRAIL exit (this is a TAKE/size overlay, not a new fire/entry/exit rule) | TAKE only, keyed on D-tier x zone-state of the detection price. OWN follows the D side: buy D -> Inzone-buy (buyer wick) / Beyond-down (below buyer zone); sell D -> Inzone-sell (seller wick) / Beyond-up (above seller zone). Body = between the wicks. Rule: **hollow** -> OWN-inzone OR body; **cyan/orange** -> OWN-inzone only; **red/green** -> OWN-inzone OR OWN-beyond. Everything else dropped | taker/taker 0.10 | in-sample (n=131, 10.1d Jun28-Jul8, three-outcome NET): **KEPT 65 = 56.9%W / 18.5%L / net +0.229%/tr / +$149** vs ALL 46.6%W / 29.8%L / +0.178% / +$234. Cuts 66 (27 losers, 24 winners — **first filter to cut MORE losers than winners**) but TOTAL drops (dropped set still nets +$85: ZZTRAIL big-winner tail). Per-tier kept: hollow 55 @ 49.1%W (+$103); cyan/orange own-inzone **5/5** (+$17); red/green own-inzone-or-beyond **5/5** (+$29). QUALITY/SIZE-UP overlay, NOT max-total: capital-normalized (~2x size on half the trades) ~+30% vs +23% with loss rate 18.5 vs 29.8 (lower DD, better for leverage). **Filled cells n=5 (fragile); bull-tape + assembled in-sample from this tape's own zone findings -> heavily optimistic, forward is the only test**; graduate n>=20/side. Study: study/pivot_composite_filter.py. **SUPERSEDED 2026-07-08 by PIVOT-COMPOSITE-v2 (below)** — v1 was D-anchor-only + quality-only ($149, total dropped); v2 uses the D-OR-entry anchor + reversed-hollow and strictly dominates baseline. |
| PIVOT-COMPOSITE-v2 (TAKE overlay, D-OR-entry) | 2026-07-08 (uncommitted) | SAME S5j-r5 fires as PIVOT-ZZTRAIL | SAME E2-tier entries + ZZTRAIL exit (TAKE overlay, not a new fire/entry/exit) | TAKE if EITHER the D zone OR the ENTRY (E2/E-held) zone satisfies the tier rule (**non-merged** single-bucket 4h wick; zone at det uses cl[det]+4h-bucket-at-det, zone at entry uses cl[j0]+4h-bucket-at-entry). OWN follows D side: buy D -> Inzone-buy (buyer wick) / Beyond-down (below buyer zone); sell D -> Inzone-sell (seller wick) / Beyond-up (above seller zone). REVERSED = the other wick. Rules: **hollow** -> REVERSED-inzone (buy in sell wick / sell in buy wick) OR body OR OWN-beyond; **cyan/orange** -> OWN-inzone only; **red/green** -> OWN-inzone OR OWN-beyond. Drop only if NEITHER anchor qualifies | taker/taker 0.10 | in-sample (n=131, 10.1d Jun28-Jul8, three-outcome NET): **KEPT 96 = 54.2%W / 24.0%L / net +0.251%/tr / +$241** vs ALL 46.6%W / 29.8%L / +0.178% / +$234 — **STRICTLY DOMINATES baseline (MORE total money AND higher win / lower loser)**. DROPPED 35 = net-negative dead weight (25.7%W / 45.7%L / -$7); cuts 16 losers vs 9 winners (cuts losers preferentially). Union beats either anchor alone (D-only 77 @ $195, entry-only 73 @ $215, **D-OR-entry 96 @ $241**). Per-tier kept: hollow 80 @ 46.2%W (+$185); cyan/orange 9 @ 88.9%W (+$26); red/green **7/7 @ 100%W** (+$31). **ASSEMBLED from this tape's own zone findings -> optimistic in-sample; filled cells n=7-9; 10-day bull tape -> forward is the ONLY honest test**; graduate n>=20/side. Study: study/pivot_composite_filter2.py |
| PIVOT-ZZTRAIL-v2 (**NEW WORKING CANDIDATE**) | 2026-07-08 (uncommitted; freeze_ts 1783518093 = Jul08 13:41 UTC) | SAME S5j-r5 fires as PIVOT-ZZTRAIL | SAME E2-tier entries + ZZTRAIL exit | = PIVOT-ZZTRAIL **+ composite-v2 TAKE filter** (take if the D zone OR the entry zone qualifies, non-merged 4h wick: hollow -> REVERSED-inzone|body|OWN-beyond; cyan/orange -> OWN-inzone; red/green -> OWN-inzone|OWN-beyond) **+ HOLLOW AVOID** (drop these side/D-zone/fill-zone combos: buy Sell-zone->Body, sell Sell-zone->Sell-zone, buy Beyond-down->Beyond-down, sell Beyond-up->Beyond-up) | taker/taker 0.10 | in-sample (n=83, 10.1d Jun28-Jul08, three-outcome NET): **60.2%W / 20.5%L / net +0.306%/tr / +25.41% / PF 6.51 / t 5.16 / 8.2 trades-day** vs frozen baseline 46.6%W/29.8%L/+0.178%/+23.38% — **dominates (more money AND far higher quality)**. Per tier kept: hollow 67 @ 52.2%W (+$198); cyan/orange 9 @ 88.9%W (+$26); red/green **7/7 @ 100%W** (+$31). **HEAVILY IN-SAMPLE-FIT: all tier rules + the 4 avoid cells (n=4-8 each) were derived from THIS same tape -> PF 6.51 / t 5.16 are inflated; FORWARD is the ONLY honest test.** v1 PIVOT-ZZTRAIL stays under its own forward test as the CONTROL. Freeze: study/out/pivot_freeze_v2.json; ruleset: study/pivot_strategy_v2.py. Pre-declared: PASS forward n>=40 & net>0 & t>=1.5; FAIL n>=40 & net<=0; degrade-warn n>=30 & net<+0.153. |

Re-run recipe per new snapshot: study/s5e_signal_exit.py (needs the S5b/S5d chain);
study/s5h_conditional.py (self-contained on the merged tape + sweep parquet);
study/pivot_backtest.py -> study/out/pivot_backtest_episodes.csv, then filter its rows: PIVOT-P2HELD =
`p2_live_at_e`>0 AND `p2_live_min_de`>-50; PIVOT-ABSORB-E = `entry_absorption`==1 (both vs the unfiltered
TP rate, over MKT/TOUCH rows only). pivot_backtest auto-globs study/data/history_snapshot_*.db so a fresh
pull is picked up with no edits. PIVOT-4HZONE = `python study/pivot_4hzone.py` (reads the tf='4h' buckets from
the newest snapshot for the 240x zones + the CSV for outcomes). PIVOT-E2-TIER = `python study/pivot_entry_timing.py`
(the STRATEGY line: cyan/orange->E2, green/red->E2, hollow->E-held+E2; reads the merged snapshot tape). NOTE the
1m aged June 22 off the 10k cap, so the forward window starts wherever the freshest snapshot's 1m begins.
**PIVOT-ZZTRAIL (the current working strategy) = `python study/pivot_forward_audit.py`** — the canonical periodic
check: reads study/out/pivot_freeze.json, runs the frozen rule, splits trades at freeze_ts (det end_time > freeze
= forward), prints PASS/FAIL/CONTINUE + a degradation flag, and APPENDS one dated row to study/out/pivot_forward_log.md
(append-only, never edited). Pull a fresh snapshot first so forward bars exist. NEVER re-tune — any param change
means a new freeze (bump pivot_freeze.json's freeze_ts, discard the prior forward log).
